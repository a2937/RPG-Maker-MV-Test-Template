if (typeof require !== "undefined" && typeof module != "undefined") {
  /*
  var chai = require("chai");
  var path = require("path");
  var fs = require("fs");
  var localDirectory = process.cwd();
  var imgDirectory = path.join(localDirectory, "img");
  var dataDirectory = path.join(localDirectory, "data");
  var enemyFile = fs.readFileSync(path.join(dataDirectory, "Enemies.json")).toString();
  var enemyData = JSON.parse(enemyFile);
  */
}

//TODO: Ensure all enemies have valid sprites and if there one is the wrong folder; explain it
